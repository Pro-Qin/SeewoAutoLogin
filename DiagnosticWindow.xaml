<Window x:Class="SeewoAutoLogin.DiagnosticWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="一键诊断" Width="520" Height="520"
    WindowStartupLocation="CenterOwner"
    Background="{StaticResource ContentBg}"
    TextElement.Foreground="{StaticResource PrimaryTextBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,14">
            <TextBlock Text="一键诊断" FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center"/>
            <Button x:Name="RunDiagnosticBtn" Content="运行诊断" Margin="14,0,0,0" Padding="16,8"
                    Click="RunDiagnostic_Click" Style="{StaticResource AccentButtonStyle}"/>
        </StackPanel>
        <ScrollViewer Grid.Row="1">
            <ItemsControl x:Name="DiagnosticResults">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,0,0,6" Padding="12,10" CornerRadius="6"
                                Background="{StaticResource BgCard}"
                                BorderBrush="{StaticResource CardBorder}" BorderThickness="1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding Icon}" FontSize="16" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="{Binding Title}" FontSize="14" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding Detail}" FontSize="12" Foreground="{StaticResource TextSecondary}"
                                               TextWrapping="Wrap"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        <TextBlock x:Name="SummaryText" Grid.Row="2" FontSize="13" Foreground="{StaticResource TextSecondary}" Margin="0,10,0,0"/>
    </Grid>
</Window>
